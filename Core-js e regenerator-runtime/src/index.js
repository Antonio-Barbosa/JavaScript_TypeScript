import 'core-js/stable' // Faço isso para navegadores mais antigos, que não suportam o java script mais recente
import 'regenerator-runtime/runtime' // Faço isso para navegadores mais antigos, que não suportam o java script mais recente
import './assets/css/style.css';
import executa from './modules/PROMISES'

executa();